'use strict';
var icejs;
var RealtimeTransportBuilder;
var Gently = require('gently');
var gently = new Gently();

icejs = require('../lib/index');
RealtimeTransportBuilder = icejs.RealtimeTransportBuilder;

exports['agent exists'] = function(test) {
    var agent = new RealtimeTransportBuilder({
        transport: {
            mode: 'rtp'
        }
    });
    test.done();
};